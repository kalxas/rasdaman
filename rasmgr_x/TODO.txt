AT:
- Send the server options to the server process
- Use the autorestart option in the server group
- User the countdown variable for restarting the server after a certain number of transaction
- Use the counter to the shared_ptr as an indicator for when the objects are in use and when they can be removed.
- Fix the client channel so that it exits in a clean way and has a well defined behavior when the server dies.
- Implement force stop for the RasServer
- Refactor the starting of the RasServer process so that I can test it. Create an abstract class: ProcessManager
- Cleanup after the rasserver process.
- Make countdown, server options, autorestart work in the server group management.
-
