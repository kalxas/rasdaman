select encode(test_grey[80:90,80:90], "csv") -- this comment text is ignored by rasdaman
from test_grey   -- for comments spanning several lines,
          -- every line needs a separate '--' starter
