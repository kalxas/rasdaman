The new tables for `petascopedb' can be added with:

$ cd ./petascope/
$ cat utilities.sql global_const.sql triggers9.sql schema9.sql | psql --set ON_ERROR_STOP=on --single-transaction petascopedb

Currently, the prefix ``ps9_'' will highlight the new tables for Petascope 9.
To *visualize* the schema structure you could look into the 
`<rasdaman>/manuals_and_examples/manuals/doc-petascope-schema/' folder for help.
To see the available tables and triggers from command line instead, you could type:

petascopedb=# \dt ps9_*
petascopedb=# \dft

To migrate the existing coverages in PS_* tables to PS9_* ones, run:

$ cd ./petascope/
$ # New Postgres session
$ cat utilities.sql global_const.sql migrate.sql | psql --set ON_ERROR_STOP=on --single-transaction petascopedb
$ # Same Postgres session
$ psql --set ON_ERROR_STOP=on --single-transaction -f migrate.sql petascopedb
