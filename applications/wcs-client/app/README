To replace the existing ows folder of petascope deployed in tomcat, e.g:
/var/lib/tomcat9/webapps/rasdaman/WEB-INF/classes/public
copy the index.html file to the ows folder first and replace the old one in tomcat,
otherwise petascope cannot find the index.html file and will return error.

